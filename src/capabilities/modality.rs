string_enum! {
    pub enum InputModality {
        Text => "text",
        Image => "image",
        Video => "video",
        Audio => "audio"
    }
}

string_enum! {
    pub enum OutputModality {
        Text => "text",
        Image => "image",
        Video => "video",
        Audio => "audio",
        Embedding => "embedding"
    }
}
